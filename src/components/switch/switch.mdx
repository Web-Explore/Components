---
name: Switch
menu: Components
---

import { Switch } from './switch.jsx'
import { Playground, Props } from 'docz'

# Test

<Playground>
  {() => {
    const options = [
      { value: 1, label: '🍌banana' },
      { value: 2, label: '🍉melon', selected: true },
      { value: 3, label: '🍊orange' },
      { value: 4, label: '💻work', disabled: true },
    ]
    return (
      <>
        <Switch
          required
          options={options}
          onChange={value => console.log(value)}
        />
        <Switch
          required
          radio
          options={options}
          onChange={value => console.log(value)}
        />
        <h2>Disabled</h2>
        <Switch
          required
          radio
          disabled
          options={options}
          onChange={value => console.log(value)}
        />
      </>
    )
  }}
</Playground>

## Props

<Props of={Switch} />
